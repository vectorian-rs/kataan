# Kataan implementation progress

| Task                                                |  Status | Notes                                                                                                                                                                                                     |
| --------------------------------------------------- | ------: | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Validate every folder has `index.md` + `index.toml` |    Done | Recursive folder validation reports missing folder index pairs, including nested folders.                                                                                                                 |
| Validate nested folders recursively                 |    Done | `validate_nested_folder_recursive` walks nested type-folder content.                                                                                                                                      |
| Validate folder index docs as documents             |    Done | Folder `index.toml`/`index.md` pairs are treated as documents with folder-path canonical IDs such as `projects/company-x`.                                                                                |
| Validate max folder depth recursively               |    Done | Recursive document/folder index metadata validation checks `max_folder_depth` and reports `folder-depth-exceeded`.                                                                                        |
| Validate type-folder mapping using top-level folder |    Done | Nested documents are checked against the top-level type folder mapping; intermediate folders do not change type.                                                                                          |
| Validate missing sidecars recursively               |    Done | Nested `.md`/`.toml` pairing checks report missing Markdown or TOML sidecars.                                                                                                                             |
| Validate edge targets against loaded document map   |    Done | Edge targets are validated against the recursively collected canonical document/type map, which is the validation-time equivalent of the loaded document set. Using `LoadedVault.documents` directly can be a later architecture cleanup, not a missing validation behavior. |
| Validate folder index drift recursively             |    Done | Recursive folder index drift and checksum validation is implemented.                                                                                                                                      |
| Add tests for nested invalid cases                  |    Done | Tests cover missing nested folder indexes, nested docs, sidecars, type mismatches, depth, checksums, and edge/ontology errors.                                                                            |
