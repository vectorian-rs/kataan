{
  "name": "kataan",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "bun --filter @kataan/web dev",
    "build:web": "bun --filter @kataan/web build",
    "check:web": "bun --filter @kataan/web check",
    "format:web": "bun --filter @kataan/web format",
    "format:check:web": "bun --filter @kataan/web format:check"
  }
}
